def RSAprogram():
    print ("What would you like to do?")
    print ("1) Encrypt message")
    print ("2) Decrypt message")
    print ("3)Exit")
    choice = raw_input("Enter an option: ")
    while raw_input != choice: 
        if choice == "1":
            e = int(raw_input("Enter your 'e' variable: "))
            n = int(raw_input("Enter your 'n' variable: "))
            encryption(e, n)
        if choice == "2":
            d = int(raw_input("Enter your 'd' variable: "))
            n = int(raw_input("Enter your 'n' variable: "))
            decryption(d, n)
        if choice == '3':
            return ("Goodbye!")
            break
        else:
            RSAprogram()
            

            
def encryption(e, n):
    encrypted_message = ""
    message = raw_input("Input Message: ")
    for x in message:
        numerize = ord(x)
        encrypt = pow(numerize, e, n)
        denumerize = unichr(encrypt)
        encrypted_message += denumerize
        
    print encrypted_message
    print ""
    enchoice = raw_input("Would you like to continue?: ")
    while raw_input != enchoice:
        if enchoice == "Yes":
            RSAprogram()
        elif enchoice == "No":
            return ("Goodbye!")
            break
def decryption(d, n):
    decrypted_message = ""
    d = int(raw_input("Enter your 'd' variable: "))
    n = int(raw_input("Enter your 'n' variable: "))
    emessage = raw_input("Input encrypted message: ")
    for t in emessage:
        numerize = ord(t)
        decrypt = pow(numerize, d, n)
        denumerize = chr(decrypt)
        decrypted_message += denumerize
    print decrypted_message
    print ""
    dechoice = raw_input("Would you like to continue?: ")
    while raw_input != dechoice:
        if dechoice == "Yes":
            RSAprogram()
        if dechoice == "No":
            return ("Goodbye!")
            break
